---
description: 新增与修改代码时必须补充中文注释
alwaysApply: true
---

# 代码注释规范

本项目要求**新增与改动的代码带上中文注释**，方便日后维护。

## 必须注释

- 新文件：顶部简短文件头（职责 / 关键行为）
- 导出函数、类型、组件 props：用 `/** ... */` 说明用途
- 非显而易见的状态、分支、副作用：行上或块前注释
- IPC 通道、持久化字段、跨窗口同步逻辑：说明「谁读谁写」

## 注释风格

- 用简体中文，写「为什么 / 约束」，少写复述代码的废话
- 与周围已有注释风格保持一致（本仓库多用 JSDoc）
- 不要给每一行都加注释；优先注释意图、边界条件、易踩坑处

## 示例

```tsx
/** null=全部；否则按标签筛选今日待办 */
const [tagFilter, setTagFilter] = useState<string | null>(null);

{/* 已按标签筛选时，列表内不再重复展示标签 chips */}
{tagFilter ? null : <TodoTagChips tags={todo.tags} />}
```

```ts
/** 发现更新后先展示日志，由用户决定是否下载 */
autoUpdater.autoDownload = false;
```
